# ProKit
A way to generate a "modern/normal" file structure for your projects, or for you to import your own.

It will not do Rust, Java, Kotlin, Swift, and Dart as they have their own. No point having `prokit rust` that just does `os.system("cargo new")`.